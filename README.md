#Install Ollama
change all the key in .env file then use  visual code from anaconda
run these commands :
## download ollama => install => from your user folder run cmd with admistration then use command : ollama run llama3.2:1b
1. conda deactivate
2. conda create venv python==3.13.5
3. pip install -r requirements.txt
4. conda activate venv
5. streamlit run olama_test.py OR streamlit run app.py
